# VWorld 노드 정리

작성일: 2026-09-12 · 구현 기준: `b6cb8c3`

저장소: [image-flow-studio](https://github.com/yuxa94/image-flow-studio)

## 1. 역할과 사용 흐름

VWorld 노드는 실제 3D 지도에 사용자의 GLB 모델을 배치하고, 원하는 구도와 비율로 캡처해 이미지 노드의 출력으로 제공한다. 이미지 생성은 연결된 Imagine 노드가 담당한다.

```text
VWorld 3D 지도 열기
  → 주소 검색 또는 지도 이동
  → GLB 모델 업로드 및 배치
  → 모델 이동·회전·크기 조정 / 기존 건물 숨기기
  → 화각과 캡처 비율 설정
  → 캡처 저장
  → VWorld 출력(out) → Imagine 입력(base)
  → Imagine에서 프롬프트 작성 및 이미지 생성
```

## 2. 실행 설정

프로젝트 루트에서 실행한다.

```bash
npm run install:all
cp server/.env.example server/.env  # 최초 설정 시에만 실행
npm run dev
```

- 클라이언트: `http://localhost:5173/`
- 서버: `http://localhost:3001/`
- `server/.env`의 `VWORLD_API_KEY`를 설정한다.
- 실행 도메인에 맞는 VWorld 키 설정이 필요하다. 키를 바꾸면 개발 서버를 재시작한다.
- Imagine 생성까지 사용하려면 Gemini API 키도 설정한다.

VWorld 지도 SDK는 브라우저에서 실행된다. 따라서 지도 SDK용 키는 HTML의 SDK 요청에 포함되며, 브라우저에서 확인할 수 있다. 주소 검색은 서버 프록시를 거친다. `.env` 파일 자체는 저장소에 커밋하지 않는다.

## 3. 화면 사용법

### 지도 열기와 위치 찾기

1. VWorld 노드의 **Open 3D Map**을 누른다. 빈 캔버스에서는 **VWorld + Imagine 시작하기**로 연결된 노드 두 개를 만들 수도 있다.
2. 주소를 검색하고 결과를 선택하거나 지도를 직접 이동한다.
3. **3D 건물**, **건물 이름** 체크박스로 지도 레이어를 조절한다.

현재 검색 UI는 도로명 주소 검색을 요청한다. 서버 API는 `type=place`도 처리하지만 UI에는 별도 장소 검색 선택 기능이 없다.

### 3D 모델 배치와 편집

1. **+ 3D 모델 올리기 (.glb)**에서 파일을 선택한다.
2. 지형이 로드된 지도 위치를 클릭해 배치한다.
3. 모델을 선택하고 이동·회전 조작 도구 또는 슬라이더로 편집한다.
4. 모델 목록의 초점 이동 버튼으로 모델을 화면 중심에 맞추거나 삭제 버튼으로 제거한다.

| 항목 | 조작 범위 / 동작 |
| --- | --- |
| Heading | 0–360° |
| Pitch | -90–90° |
| Roll | -180–180° |
| Scale | 0.1–10배 |
| 모델 이동 | 지도 클릭으로 재배치하거나 축을 드래그 |
| 변형 초기화 | 회전과 배율 초기화 |
| 화각 | 10–120°, 기본 60° |

모델 회전과 배율 변경은 모델 중심을 기준으로 처리한다. GLB의 원점이 실제 형상에서 멀리 떨어져 있어도 원점을 중심으로 공전하지 않도록 보정한다.

### 기존 지도 건물 숨기기

- **건물 선택해 숨기기**를 누른 뒤 VWorld 건물을 클릭한다.
- 숨긴 건물 목록에서 개별 복원하거나 전체 복원할 수 있다.
- 전체 3D 건물 레이어를 끄는 기능과 개별 건물을 숨기는 기능은 별개다.
- 식별자가 있는 건물은 줌아웃·줌인으로 타일이 교체되어도 숨김 상태를 다시 적용한다.
- 사용자가 올린 GLB 모델은 모델 목록에서 별도로 삭제한다.

### 비율 선택과 캡처

1. **비율 선택 및 캡처**로 캡처 화면에 들어간다.
2. 지도를 움직이거나 화각을 바꿔 구도를 정한다.
3. 비율을 선택하고 **캡처 영역 이동 / 크기 조절**로 프레임을 조정한다.
4. **캡처 저장**을 누르면 이미지가 VWorld 노드에 표시된다.
5. **Imagine에 연결**을 눌러 Base 입력으로 전달한다. 이미 연결된 Imagine이 있으면 재사용한다.

비율 프리셋: `1:1`, `2:3`, `3:2`, `3:4`, `4:3`, `4:5`, `5:4`, `9:16`, `16:9`, `21:9`. 기본값은 `16:9`이다. 전체 화면 옵션에서는 원본 지도 캔버스를 사용한다.

캡처 해상도는 지도 캔버스와 선택 영역의 픽셀 크기에 따라 정해진다. Imagine의 생성 해상도 설정과는 별개다. Imagine에서 비율을 `AUTO`로 두면 연결된 캡처 비율이 생성 요청에 사용된다.

## 4. 데이터와 상태

VWorld 노드는 입력 핸들 없이 오른쪽 출력 핸들 `out`을 제공한다.

```js
{
  id: 'vworld-...',
  type: 'vworld',
  position: { x: 80, y: 80 },
  data: {
    output: null,          // 캡처 후 PNG data URL
    captureRatio: '16:9'  // 전체 화면 캡처에서는 null
  }
}
```

- `vworldOpen`: 지도 모달 열림 상태
- `vworldTargetNodeId`: 현재 캡처를 받을 VWorld 노드 ID
- `setNodeOutput`: 캡처 이미지를 저장하고 연결된 입력에 전파
- `connectToImagine`: 기존 연결 재사용 또는 Imagine 생성 및 `out → base` 연결
- `models`: 지도 모달에서 모델의 primitive, 위치, 중심점, 회전, 배율 관리

지도 뷰어는 페이지 전체에서 하나를 공유한다. 여러 VWorld 노드가 각각 독립된 지도 장면을 저장하는 구조는 아니다. 각 노드는 자신의 캡처 출력 이미지를 갖는다. 지도·모델 배치 및 노드 그래프의 영구 저장 기능은 현재 구현되어 있지 않으므로 새로고침 전 필요한 이미지를 다운로드한다.

VWorld 노드에서 다시 캡처하면 해당 노드의 출력은 최신 이미지로 교체된다. Imagine의 생성 결과를 별도 이미지 노드로 누적하는 기능은 이 캡처 동작과 구분된다.

## 5. 구현 파일

아래 경로는 저장소 루트 기준이다.

| 파일 | 역할 |
| --- | --- |
| `client/src/nodes/VWorldNode.jsx` | 캡처 미리보기·다운로드, 지도 열기, Imagine 연결, 출력 핸들 |
| `client/src/components/VWorldMapModal.jsx` | 지도 초기화, 모델 편집, 건물 숨김 UI, 검색, 캡처 |
| `client/src/lib/vworld.js` | SDK 대기, 모델 로드, 건물 숨김 유지·복원 |
| `client/src/lib/modelGeometry.js` | 모델 중심 보정, GLB 검정 모서리 생성 |
| `client/src/lib/ratios.js` | 공통 비율 프리셋, 화면 크기에 맞는 캡처 영역 계산 |
| `client/src/lib/imageUtils.js` | 캡처 이미지 자르기 |
| `client/src/lib/store.js` | 모달 상태, 노드 출력 전파, Imagine 연결 |
| `client/src/App.jsx` | 노드 등록, 시작 버튼, 공유 지도 모달 배치 |
| `client/index.html` | VWorld WebGL SDK 스크립트 로드 |
| `client/vite.config.js` | 환경변수 주입, 개발용 `/api` 프록시 |
| `server/index.js` | `GET /api/vworld/search` 주소 검색 프록시 |

### 모델 중심 보정

`withModelPivot`은 Cesium 모델의 bounding sphere 중심을 월드 좌표에서 가져와 모델 좌표계의 중심점으로 변환한다. 여기서 중심은 렌더링 경계 구의 중심이며 질량 중심은 아니다.

`keepModelPivotFixed`는 회전·배율 변경 시 아래 관계로 변환 행렬의 이동 성분을 보정한다.

```text
이동 성분 = 고정할 월드 중심 - 회전 행렬 × (배율 × 로컬 중심)
```

슬라이더와 회전·이동 조작 도구가 같은 중심 보정을 사용한다.

### 검정 모서리

기존 화면 후처리 외곽선 대신 업로드 시 GLB에 실제 선 primitive를 추가한다.

- 원본 면과 재질을 유지하면서 경계와 꺾이는 모서리를 추출한다.
- 검정색 재질의 glTF `LINES`를 추가하고 원본 선 primitive도 검정색으로 설정한다.
- 한 평면을 삼각형으로 나누는 내부 대각선은 생략한다. 모든 삼각형을 표시하는 와이어프레임 모드는 아니다.
- 정적 모델의 새 선은 표면에서 조금 띄워 깊이 충돌을 줄인다.
- 화면에서 선택해도 검정 선을 유지한다. 이전 노란 선택 외곽선 후처리는 사용하지 않는다.

### 줌 변경 후 건물 숨김 유지

건물 객체 참조 대신 `TD_ID` 또는 `MODEL_NAME`을 키로 기록한다. `tileVisible`에서 숨김을 적용해 캐시된 타일의 재등장과 스타일 재적용에 대응한다. 복합 타일과 중첩 타일셋도 순회하며, 복원 시 현재 로드된 타일에 반영한다.

## 6. 지원 범위와 확인할 점

- 업로드 UI는 `.glb`만 받는다. `.gltf`는 GLB로 변환해야 한다.
- 검정 모서리 처리는 단일 내장 바이너리 버퍼를 가진 GLB 2.0을 대상으로 한다.
- Draco·Meshopt 압축이나 sparse accessor를 사용하는 형상은 현재 모서리 처리에서 지원하지 않는다. 압축을 해제한 GLB로 내보내야 한다.
- 새 모서리 처리를 적용하려면 이전에 로드한 모델을 삭제하고 다시 업로드해야 한다.
- 작은 선은 거리와 화면 해상도에 따라 계단이나 끊김이 보일 수 있다. 깊이 보정은 이를 완전히 없애는 안티앨리어싱 처리는 아니다.
- 지도 SDK·지형·건물 데이터 로딩은 외부 VWorld 서비스 상태에 영향을 받는다.
- 식별자가 없는 지도 객체는 개별 건물 숨김 유지 대상으로 등록할 수 없다.

## 7. 수정 및 검증 기록

| 항목 | 적용 내용 |
| --- | --- |
| 건물 숨김 | `tileVisible` 기반으로 줌·타일 교체 후 숨김 재적용 |
| 모델 로드 | 구형·신형 Cesium 로드 API 분기, 시간 제한 및 실패 시 정리 |
| 회전 중심 | GLB 원점과 형상 중심이 다른 모델의 회전·배율 중심 보정 |
| 모서리 | 화면 외곽선에서 실제 검정 선 geometry로 변경 |
| 캡처 | 실제 지도 캔버스 사용, 화면 크기에 맞춘 비율 프레임 계산 |
| Imagine 연결 | 캡처 이미지와 비율 전달, 기존 연결 재사용 |

제공된 `high profile mass.glb`로 모델 업로드, Heading 90°·Pitch 30°·Roll 20° 회전, 검정 모서리 표시, 16:9 캡처 및 Imagine Base 전달을 브라우저에서 확인했다. 사용자 모델 파일은 저장소에 포함하지 않았다.

관련 테스트:

- `tests/model-geometry.test.mjs`: 중심 고정, 평면 대각선 제외, 꺾인 모서리 생성, 원본 선의 검정 재질, 원본 형상 보존
- `tests/model-loading.test.mjs`: 모델 로드 및 실패 정리
- `tests/vworld-visibility.test.mjs`: 타일·스타일 변경 후 숨김, 복원, 이벤트 해제
- `tests/workflow.test.mjs`: 노드 입력 전파와 비율·연결 관련 회귀 검증

```bash
node --test tests/*.test.mjs
npm run build --prefix client
```

구현 기준 커밋에서 전체 자동 테스트 33개와 클라이언트 빌드가 통과했다.
